<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PersonalWinUIApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PersonalWinUIApp"
    xmlns:font="using:MyWinUIUtilities.Tools"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" x:Name="mainWindow" Closed="mainWindow_Closed">

    <!--
    LayerOnAcrylicFillColorDefault
    LayerOnAccentAcrylicFillColorDefault
    SystemControlAcrylicElementBrush
    Background="#E8E8E8"
    Background="#202020"
    -->

    <Grid x:Name="MainAppGrid">

        <Grid.RowDefinitions>
            <RowDefinition Height="46"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- * Title Part * -->

        <TitleBar x:Name="TitleApp" Title="{Binding [ApplicationTitle], Source={StaticResource Loc}}" Subtitle="{Binding [ApplicationSubtitle], Source={StaticResource Loc}}" Grid.Row="0">
            <TitleBar.IconSource>
                <SymbolIconSource Symbol="Favorite"/>
            </TitleBar.IconSource>
            <TitleBar.Content>
                <AutoSuggestBox x:Name="MainSuggestBox" Width="300" PlaceholderText="{Binding [Searching], Source={StaticResource Loc}}" QueryIcon="Find" VerticalContentAlignment="Center" FontFamily="{Binding CurrentFont, Source={StaticResource FontServices}}" font:FontScaleHelper.Enable="True"/>
            </TitleBar.Content>
            <TitleBar.RightHeader>
                <Button x:Name="AccountPageButton" Background="{ThemeResource ControlAltFillColorDisabled}" BorderThickness="0" Click="AccountPageButton_Click">
                    <PersonPicture Width="30" Initials="NP" Background="{ThemeResource SystemAccentColor}"/>
                </Button>
            </TitleBar.RightHeader>
        </TitleBar>

        <!-- * ShellPage Part * -->

        <ContentPresenter Grid.Row="1" x:Name="ShellHost" />

    </Grid>
</Window>
